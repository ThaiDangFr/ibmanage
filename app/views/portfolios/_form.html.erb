<%= form_with(model: portfolio, local: true) do |form| %>
  <% if portfolio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(portfolio.errors.count, "error") %> prohibited this portfolio from being saved:</h2>

      <ul>
      <% portfolio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name , :class => "col-md-4 control-label" %>
    <%= form.text_field :name , :class => "form-control input-md" %>
  </div>

  <div class="form-group">
    <%= form.label :token , :class => "col-md-4 control-label" %>
    <%= form.text_field :token , :class => "form-control input-md" %>
  </div>

  <div class="action">
    <%= form.submit class: 'btn btn-info' %>
  </div>
<% end %>
